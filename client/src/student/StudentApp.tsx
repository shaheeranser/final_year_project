/**
 * StudentApp.tsx
 *
 * Entry point for the student-side code bundle.
 * Lazy-loaded from the top-level App router.
 */

import { ExamPage } from './pages/ExamPage';

export default function StudentApp() {
  return <ExamPage />;
}
